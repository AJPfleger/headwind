{% extends "base.html.j2" %}

{% block maincol %}

<h1 class="title">
{{ metric.name }}
</h1>

<hr/>

{# <nav class="breadcrumb" aria-label="breadcrumbs">
  <ul>
    {% for part in current_url().parts %}
    <li><a href="#">{{ part }}</a></li>
    {% endfor %}
  </ul>
</nav> #}

{% for plot in plots %}
<img src="{{ url_for(plot) }}" />
{% endfor %}

<div class="content">
{% for g, df in dataframe.groupby("branch") %}
<h2 class="title">{{g}}</h2>
{{ df[["commit", "date", "value"]].to_html() }}
{% endfor %}
</div>

{% endblock %}